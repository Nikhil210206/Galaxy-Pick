name: galaxy-pick
channels: [conda-forge]
dependencies:
  - python=3.11
  - pandas
  - numpy
  - matplotlib
  - jupyter
  - streamlit
  - pytest
  - python-dotenv
  - pip
  - pip:
      - google-generativeai   # optional; only used if a key is set
